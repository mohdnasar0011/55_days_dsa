class Solution {
    public boolean containsDuplicate(int[] nums) {
        boolean a = false;
        HashSet<Integer> map = new HashSet<>();

        for (int num : nums) {
            if (map.contains(num))
                a = true;
            else {
                map.add(num);
            }
        }
        return a;

    }
}

